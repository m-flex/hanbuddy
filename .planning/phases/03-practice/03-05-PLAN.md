---
phase: 03-practice
plan: 05
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/components/practice/SentenceTiles.tsx
  - src/components/practice/SentenceTiles.test.tsx
  - src/components/practice/DialoguePlayer.tsx
  - src/components/practice/DialoguePlayer.test.tsx
  - src/pages/SentenceBuildPage.tsx
  - src/pages/DialogueLessonPage.tsx
autonomous: true
requirements: [GRAM-03, LIST-03]

must_haves:
  truths:
    - "User can tap word tiles to assemble a Korean sentence in order"
    - "Tapping a placed tile returns it to the pool"
    - "Correct answer is checked against the canonical sentence from vocab data"
    - "Incorrect answer shows correct word order + full sentence with translation"
    - "User can see dialogue lines with speaker labels and play audio for each line"
    - "Dialogues have 4-8 lines with Korean, English, and romanization"
  artifacts:
    - path: "src/components/practice/SentenceTiles.tsx"
      provides: "Tap-to-place tile pool and answer track"
      min_lines: 40
    - path: "src/components/practice/SentenceTiles.test.tsx"
      provides: "Tests for tile state transitions"
      min_lines: 30
    - path: "src/pages/SentenceBuildPage.tsx"
      provides: "10-question sentence building exercise"
      min_lines: 60
    - path: "src/components/practice/DialoguePlayer.tsx"
      provides: "Dialogue display with per-line audio"
      min_lines: 40
    - path: "src/pages/DialogueLessonPage.tsx"
      provides: "Page listing and displaying dialogues"
      min_lines: 40
  key_links:
    - from: "src/pages/SentenceBuildPage.tsx"
      to: "src/components/practice/SentenceTiles.tsx"
      via: "tile interaction component"
      pattern: "SentenceTiles"
    - from: "src/pages/SentenceBuildPage.tsx"
      to: "src/components/practice/ExerciseProgress.tsx"
      via: "progress bar"
      pattern: "ExerciseProgress"
    - from: "src/components/practice/DialoguePlayer.tsx"
      to: "src/components/ui/AudioButton.tsx"
      via: "per-line audio playback"
      pattern: "AudioButton"
    - from: "src/pages/DialogueLessonPage.tsx"
      to: "src/data/dialogues.ts"
      via: "dialogue content data"
      pattern: "DIALOGUES"
---

<objective>
Build the sentence-building exercise with tap-to-place tiles and the dialogue lesson viewer with per-line audio playback.

Purpose: Sentence building (GRAM-03) practices Korean word order through active construction. Dialogue lessons (LIST-03) provide conversational listening and reading practice with real multi-turn Korean conversations.
Output: Working SentenceBuildPage at /practice/build and DialogueLessonPage.
</objective>

<execution_context>
@C:/Users/Marcu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Marcu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-practice/03-CONTEXT.md
@.planning/phases/03-practice/03-RESEARCH.md
@.planning/phases/03-practice/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (dialogue data) -->

From src/types/content.ts (extended by Plan 01):
```typescript
export interface DialogueLine {
  speaker: string;
  korean: string;
  english: string;
  romanization: string;
}

export interface Dialogue {
  id: `dlg-${number}`;
  title: string;
  description: string;
  topics: `top-${number}`[];
  lines: DialogueLine[];
  vocab_ids: `voc-${number}`[];
}

export interface VocabItem {
  // ... all fields including examples: ExampleSentence[]
}

export interface ExampleSentence {
  korean: string;
  english: string;
  romanization: string;
}
```

From src/data/dialogues.ts (created by Plan 01):
```typescript
export const DIALOGUES: Dialogue[];  // 3+ dialogues with 4-8 lines each
```

From src/components/practice/ExerciseProgress.tsx (Plan 02):
```typescript
export default function ExerciseProgress({ current, total }: { current: number; total: number }): JSX.Element;
```

From src/utils/audio.ts:
```typescript
export function speak(text: string, speed?: number): Promise<void>;
```

From src/components/ui/AudioButton.tsx:
```typescript
export default function AudioButton({ text, speed, size }: { text: string; speed?: number; size?: number }): JSX.Element;
```

From src/utils/exerciseUtils.ts (Plan 04, but this plan does NOT depend on it — sentence build generates its own questions):
```typescript
// NOTE: SentenceBuildPage generates its own questions from vocab examples
// It does NOT use exerciseUtils — word splitting is specific to sentence building
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SentenceTiles component and SentenceBuildPage with tile interaction</name>
  <files>src/components/practice/SentenceTiles.tsx, src/components/practice/SentenceTiles.test.tsx, src/pages/SentenceBuildPage.tsx</files>
  <behavior>
    - SentenceTiles renders pool tiles and placed tiles in two distinct areas
    - Tapping a pool tile moves it to the end of placed array
    - Tapping a placed tile returns it to the pool
    - "Check" button compares placed tiles joined by space to the canonical Korean sentence
    - Correct: green highlight on all placed tiles
    - Incorrect: red highlight on placed tiles, show correct sentence below
  </behavior>
  <action>
1. Create `src/components/practice/SentenceTiles.tsx`:
   - Per RESEARCH.md Pattern 5 (Tap-to-Place Sentence Tiles)
   - Props: `{ words: string[]; onComplete: (userAnswer: string) => void }`
   - Internal state: `pool` (string[] — shuffled words) and `placed` (string[] — user's answer order)
   - Pool area (top): display each word as a tappable tile (rounded-lg border px-4 py-2 bg-blue-50 text-lg). Tap moves word from pool to end of placed.
   - Placed area (bottom): horizontal flex-wrap area with min-height placeholder. Each placed tile is tappable — tap returns it to pool.
   - "Check" button (disabled until all tiles placed): calls onComplete(placed.join(' '))
   - Tailwind: pool tiles bg-blue-50 border-blue-200, placed tiles bg-white border-blue-400, placed area has dashed border placeholder when empty
   - Reset function to re-shuffle and clear placed (for "Try Again")

2. Create `src/components/practice/SentenceTiles.test.tsx`:
   - Test initial render shows all words in pool, none in placed
   - Test tapping pool tile moves it to placed
   - Test tapping placed tile returns it to pool
   - Test check button disabled until all tiles placed
   - Use @testing-library/react with fireEvent.click

3. Create/replace `src/pages/SentenceBuildPage.tsx`:
   - On mount: get unlocked vocab by inlining the getUnlockedVocab logic directly (import LESSONS and VOCAB, collect vocab_ids from completedLessons, deduplicate, map to VocabItem). Do not import from exerciseUtils — Plan 04 creates that file in parallel and it may not exist yet
   - Generate up to 10 sentence-building questions:
     - For each question: pick a vocab item, pick an example sentence
     - Split the Korean sentence into word tokens: `sentence.korean.split(' ')` (Korean words are space-separated)
     - The canonical answer is the original sentence
     - Per RESEARCH.md Pitfall 3: only use short sentences (3-5 tokens). Filter example sentences to those with <= 5 space-separated tokens. If not enough, allow up to 7 tokens.
     - English prompt is the example.english
   - Question type: `{ vocabId: string; englishPrompt: string; koreanWords: string[]; correctSentence: string }`
   - Display: back arrow to /practice, ExerciseProgress, English prompt, SentenceTiles
   - On complete: compare user answer to correctSentence (trim both, normalize NFC)
   - Correct: green flash, auto-advance after 1s
   - Incorrect: show correct word order as a highlighted sentence, "Next" button
   - Results screen after all questions: score/total, "Practice Again" and "Back to Practice"
   - Empty state guard: if no unlocked vocab, show "Complete a lesson first" with Link to /
  </action>
  <verify>
    <automated>npx vitest run src/components/practice/SentenceTiles.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>SentenceTiles manages pool/placed state with tap-to-place and tap-to-remove. SentenceBuildPage generates word-order questions from short example sentences. Correct/incorrect feedback works. All tests pass. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: DialoguePlayer component and DialogueLessonPage</name>
  <files>src/components/practice/DialoguePlayer.tsx, src/components/practice/DialoguePlayer.test.tsx, src/pages/DialogueLessonPage.tsx, src/main.tsx</files>
  <action>
1. Create `src/components/practice/DialoguePlayer.tsx`:
   - Props: `{ dialogue: Dialogue }`
   - Render each DialogueLine as a chat-bubble style row:
     - Speaker name in bold (small, above the bubble)
     - Korean text in the bubble (bg-blue-50 for speaker A, bg-gray-50 for speaker B — alternate by speaker)
     - English translation below in gray text-sm
     - AudioButton at the right of each bubble to play that line's Korean text
   - Romanization: show inline in text-xs gray below Korean if user has romanization setting enabled (check useRomanizationToggle or just show it — per Claude's discretion, show it by default on dialogues since they are longer sentences)
   - Tailwind: chat-style layout with alternating alignment (speaker A left, speaker B right) or consistent left-alignment with color differentiation

2. Create `src/components/practice/DialoguePlayer.test.tsx`:
   - Test renders all dialogue lines with speaker names
   - Test renders AudioButton for each line
   - Test renders English translations
   - Mock AudioButton to avoid TTS calls

3. Create `src/pages/DialogueLessonPage.tsx`:
   - Route: /practice/dialogues (new route — will add to main.tsx)
   - List view: show all DIALOGUES as cards with title, description, topic badges, line count
   - On card click: expand inline or navigate to show DialoguePlayer for that dialogue
   - Simpler approach: single scrollable page. Top: dialogue selector (tabs or cards). Below: DialoguePlayer for selected dialogue.
   - Back arrow to /practice
   - Import DIALOGUES from src/data/dialogues.ts

4. Update `src/main.tsx`:
   - Add route: `{ path: 'practice/dialogues', element: <DialogueLessonPage /> }`
   - Import DialogueLessonPage

5. Update `src/pages/PracticeDashboardPage.tsx` (minor):
   - Add a 4th card below the exercise cards: "Dialogues" card linking to /practice/dialogues
   - Show dialogue count from DIALOGUES.length
   - This is a small addition (3-5 lines of JSX) to the existing dashboard
  </action>
  <verify>
    <automated>npx vitest run src/components/practice/DialoguePlayer.test.tsx --reporter=verbose && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>DialoguePlayer renders chat-style dialogue lines with per-line AudioButton. DialogueLessonPage lists all dialogues and shows selected dialogue. Route /practice/dialogues works. PracticeDashboardPage has Dialogues card. All tests pass. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` — all tests pass
- `npm run build` succeeds
- /practice/build renders sentence-building exercise with tap-to-place tiles
- /practice/dialogues renders dialogue list and player with per-line audio
- PracticeDashboardPage shows Dialogues card
</verification>

<success_criteria>
- SentenceTiles: tap pool tile adds to placed, tap placed tile returns to pool
- SentenceBuildPage: generates word-order questions from short examples, checks canonical order
- Incorrect sentence shows correct word order + full sentence
- DialoguePlayer renders 4-8 line conversations with speaker labels and per-line AudioButton
- DialogueLessonPage lists and displays all dialogues from data
- All new routes registered and accessible
</success_criteria>

<output>
After completion, create `.planning/phases/03-practice/03-05-SUMMARY.md`
</output>
