---
phase: 03-practice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/content.ts
  - src/data/vocab.ts
  - src/data/dialogues.ts
  - src/types/content.test.ts
  - package.json
autonomous: true
requirements: [VOCAB-04, VOCAB-05, LIST-03]

must_haves:
  truths:
    - "Every vocab item has a topik_level field ('beginner' or 'intermediate')"
    - "Every vocab item has 2-3 example sentences with Korean, English, and romanization"
    - "At least 2 dialogue datasets exist with 4-8 lines each, speakers, Korean+English+romanization per line"
    - "ts-fsrs is installed and importable"
  artifacts:
    - path: "src/types/content.ts"
      provides: "VocabItem with topik_level field, DialogueLine and Dialogue types"
      contains: "topik_level"
    - path: "src/data/vocab.ts"
      provides: "20 vocab items all with examples and topik_level"
      min_lines: 300
    - path: "src/data/dialogues.ts"
      provides: "At least 2 authored dialogues with playable lines"
      min_lines: 40
    - path: "package.json"
      provides: "ts-fsrs dependency"
      contains: "ts-fsrs"
  key_links:
    - from: "src/data/vocab.ts"
      to: "src/types/content.ts"
      via: "satisfies VocabItem[]"
      pattern: "satisfies VocabItem"
    - from: "src/data/dialogues.ts"
      to: "src/types/content.ts"
      via: "satisfies Dialogue[]"
      pattern: "satisfies Dialogue"
---

<objective>
Install ts-fsrs, extend the content schema with TOPIK level and dialogue types, author missing vocab examples, and create dialogue content data.

Purpose: All practice features depend on complete vocab data (examples on every item, TOPIK tags) and dialogue content. This data foundation must exist before any exercise UI is built.
Output: Extended types, complete vocab data, authored dialogues, ts-fsrs available.
</objective>

<execution_context>
@C:/Users/Marcu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Marcu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-practice/03-CONTEXT.md
@.planning/phases/03-practice/03-RESEARCH.md

<interfaces>
<!-- Key types the executor needs -->

From src/types/content.ts:
```typescript
export type SpeechLevel = 'formal-high' | 'polite' | 'informal' | 'plain';
export type ConjugationType = 'action' | 'descriptive' | 'noun' | 'particle' | 'expression';

export interface ExampleSentence {
  korean: string;
  english: string;
  romanization: string;
}

export interface VocabItem {
  id: `voc-${number}`;
  korean: string;
  english: string;
  romanization: string;
  speech_level: SpeechLevel;
  conjugation_type: ConjugationType;
  topics: `top-${number}`[];
  examples?: ExampleSentence[];
}

export interface Lesson {
  id: `les-${number}`;
  title: string;
  description: string;
  level: number;
  order: number;
  vocab_ids: `voc-${number}`[];
  grammar_ids: `grm-${number}`[];
  topics: `top-${number}`[];
}
```

From src/data/vocab.ts:
- 20 vocab items (voc-001 through voc-020)
- Items MISSING examples: voc-006 (사람), voc-008 (어디), voc-009 (네), voc-010 (아니요), voc-013 (셋)
- Uses `satisfies VocabItem[]` pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ts-fsrs and extend content types with TOPIK level and dialogue schema</name>
  <files>package.json, src/types/content.ts</files>
  <action>
1. Run `npm install ts-fsrs` to add the FSRS v6 scheduling library.

2. In `src/types/content.ts`, add:
   - `export type TopikLevel = 'beginner' | 'intermediate';` after ConjugationType
   - Add `topik_level: TopikLevel;` field to `VocabItem` interface (required, not optional — all 20 items will be tagged)
   - Change `examples?: ExampleSentence[]` to `examples: ExampleSentence[]` (required — all items will have examples after this phase)
   - Add new types at the bottom of the file:

```typescript
/** A single line in a Korean dialogue. */
export interface DialogueLine {
  speaker: string;
  korean: string;
  english: string;
  romanization: string;
}

/** A dialogue lesson with 4-8 lines of conversation. */
export interface Dialogue {
  id: `dlg-${number}`;
  title: string;
  description: string;
  /** Topic references */
  topics: `top-${number}`[];
  /** The dialogue lines in order */
  lines: DialogueLine[];
  /** Vocab IDs referenced in this dialogue */
  vocab_ids: `voc-${number}`[];
}
```

Do NOT modify any other types. The `satisfies` operator in data files will catch type mismatches at build time.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>ts-fsrs appears in package.json dependencies. VocabItem has required topik_level and required examples fields. DialogueLine and Dialogue types are exported from content.ts. tsc may show errors from data files (expected — Task 2 fixes them).</done>
</task>

<task type="auto">
  <name>Task 2: Author missing vocab examples, add TOPIK levels to all items, create dialogue data, and update tests</name>
  <files>src/data/vocab.ts, src/data/dialogues.ts, src/types/content.test.ts</files>
  <action>
1. In `src/data/vocab.ts`, add `topik_level: 'beginner'` to all 20 items (all existing vocab is TOPIK-I Level 1-2 beginner content).

2. Author example sentences for the 5 items missing them. Use natural beginner-level Korean with accurate romanization:

   - **voc-006 (사람/Person):**
     - "그 사람은 학생이에요." / "That person is a student." / "Geu sarameun haksaengieyo."
     - "좋은 사람이에요." / "They are a good person." / "Joeun saramieyo."

   - **voc-008 (어디/Where):**
     - "어디에 가요?" / "Where are you going?" / "Eodie gayo?"
     - "화장실이 어디에 있어요?" / "Where is the restroom?" / "Hwajangsiri eodie isseoyo?"

   - **voc-009 (네/Yes):**
     - "네, 맞아요." / "Yes, that's right." / "Ne, majayo."
     - "네, 알겠습니다." / "Yes, I understand." / "Ne, algesseumnida."

   - **voc-010 (아니요/No):**
     - "아니요, 괜찮아요." / "No, it's okay." / "Aniyo, gwaenchanayo."
     - "아니요, 감사합니다." / "No, thank you." / "Aniyo, gamsahamnida."

   - **voc-013 (셋/Three):**
     - "셋 다 주세요." / "Please give me all three." / "Set da juseyo."
     - "우리는 셋이에요." / "There are three of us." / "Urineun sesieyo."

   Make `examples` required (not optional) on every item — remove the `?` was already done in Task 1 types.

3. Create `src/data/dialogues.ts` with at least 3 dialogues using the `satisfies Dialogue[]` pattern:

   **Dialogue 1 (dlg-001): "At the Coffee Shop"** — topic top-003 (Food & Drink)
   - 6 lines between Customer and Barista
   - Uses voc-016 (커피), voc-018 (주세요), voc-019 (얼마예요?)
   - Natural ordering flow: greeting, order, confirm, price, payment, thanks

   **Dialogue 2 (dlg-002): "Meeting Someone New"** — topic top-001 (Greetings)
   - 6 lines between Person A and Person B
   - Uses voc-001 (안녕하세요), voc-009 (네), voc-003 (감사합니다)
   - Introduction flow: greeting, name exchange, nice to meet you

   **Dialogue 3 (dlg-003): "Asking for Directions"** — topic top-005 (Travel)
   - 4 lines between Traveler and Local
   - Uses voc-008 (어디), voc-020 (실례합니다), voc-003 (감사합니다)
   - Ask directions, get answer, thank

4. Update `src/types/content.test.ts` to add tests:
   - Every vocab item has `topik_level` that is 'beginner' or 'intermediate'
   - Every vocab item has at least 2 examples
   - At least 3 dialogues exist
   - Each dialogue has 4-8 lines
   - Each dialogue line has speaker, korean, english, romanization (all non-empty strings)
   - Import DIALOGUES from data file in test

  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 20 vocab items have topik_level: 'beginner' and at least 2 example sentences. 3 dialogues authored in dialogues.ts. All tests pass including new content coverage tests. tsc --noEmit clean. npm run build passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npx vitest run` — all tests pass
- `npm run build` succeeds
- `import { createEmptyCard, fsrs, Rating } from 'ts-fsrs'` resolves without error
</verification>

<success_criteria>
- ts-fsrs installed and importable
- VocabItem type has required `topik_level: TopikLevel` and required `examples: ExampleSentence[]`
- All 20 vocab items have topik_level and 2+ examples
- DialogueLine and Dialogue types exported
- 3 authored dialogues in src/data/dialogues.ts
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-practice/03-01-SUMMARY.md`
</output>
