---
phase: 02-lessons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tts-worker/worker.js
  - tts-worker/wrangler.toml
  - .env.local
  - src/utils/audio.ts
  - src/utils/audio.test.ts
  - src/utils/lessonUnlock.ts
  - src/utils/lessonUnlock.test.ts
  - src/utils/lucideIcons.ts
  - src/components/layout/RootLayout.tsx
  - src/components/layout/BottomTabBar.tsx
  - src/components/ui/AudioButton.tsx
  - src/components/ui/SpeedToggle.tsx
  - src/components/ui/SpeechBadge.tsx
  - src/components/ui/WelcomeCard.tsx
  - src/components/ui/CompletionCard.tsx
  - src/components/ui/Toast.tsx
  - src/components/ui/SpeechBadge.test.tsx
  - src/components/ui/WelcomeCard.test.tsx
  - src/pages/LessonListPage.tsx
  - src/pages/TopicListPage.tsx
  - src/pages/SettingsPage.tsx
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements: [LIST-01, PROG-05]

must_haves:
  truths:
    - "Audio plays Korean text at both 0.75x and 1.0x speed via Naver TTS worker"
    - "Speed toggle persists across audio plays within a session"
    - "Bottom tab bar navigates between Lessons, Topics, and Settings"
    - "WelcomeCard appears when no lessons are completed"
    - "Locked lesson tap shows a toast message"
  artifacts:
    - path: "src/utils/audio.ts"
      provides: "speak() and stopAudio() with blob-URL caching and single-instance cancellation"
      exports: ["speak", "stopAudio"]
    - path: "src/utils/lessonUnlock.ts"
      provides: "isLessonUnlocked() pure function"
      exports: ["isLessonUnlocked"]
    - path: "src/components/layout/RootLayout.tsx"
      provides: "Root layout with Outlet and BottomTabBar"
      exports: ["default"]
    - path: "src/components/ui/AudioButton.tsx"
      provides: "Speaker icon button calling speak()"
      exports: ["AudioButton"]
    - path: "src/components/ui/SpeechBadge.tsx"
      provides: "Colored badge for speech level"
      exports: ["SpeechBadge"]
    - path: "tts-worker/worker.js"
      provides: "Cloudflare Worker for Naver TTS proxy"
  key_links:
    - from: "src/components/ui/AudioButton.tsx"
      to: "src/utils/audio.ts"
      via: "speak() import"
      pattern: "import.*speak.*from.*utils/audio"
    - from: "src/components/ui/SpeedToggle.tsx"
      to: "src/store/settings.ts"
      via: "useSettingsStore audioSpeed"
      pattern: "useSettingsStore.*audioSpeed"
    - from: "src/main.tsx"
      to: "src/components/layout/RootLayout.tsx"
      via: "createBrowserRouter layout route"
      pattern: "createBrowserRouter"

user_setup:
  - service: cloudflare-workers
    why: "TTS audio proxy for Naver Korean voice API"
    env_vars:
      - name: VITE_TTS_WORKER_URL
        source: "Cloudflare Workers dashboard after deploying tts-worker/ with `npx wrangler deploy`"
    dashboard_config:
      - task: "Deploy TTS worker"
        location: "Run `cd tts-worker && npx wrangler deploy` — or use Hanguller's worker URL temporarily"
---

<objective>
Set up the routing shell, audio infrastructure, and shared UI components that all lesson/topic pages depend on.

Purpose: Every page in Phase 2 needs the router, audio playback, and reusable UI atoms. Building these first lets Plans 03 and 04 focus purely on page-level composition.
Output: Working app shell with tab navigation, audio utility, and all shared components ready for consumption.
</objective>

<execution_context>
@C:/Users/Marcu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Marcu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lessons/02-CONTEXT.md
@.planning/phases/02-lessons/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 1 that this plan consumes -->

From src/types/content.ts:
```typescript
export type SpeechLevel = 'formal-high' | 'polite' | 'informal' | 'plain';
export interface ExampleSentence {
  korean: string;
  english: string;
  romanization: string;
}
export interface Lesson {
  id: `les-${number}`;
  title: string;
  description: string;
  level: number;
  order: number;
  vocab_ids: `voc-${number}`[];
  grammar_ids: `grm-${number}`[];
  topics: `top-${number}`[];
}
export interface Topic {
  id: `top-${number}`;
  name: string;
  description: string;
  icon?: string;
}
```

From src/types/store.ts:
```typescript
export interface SettingsState {
  audioEnabled: boolean;
  audioSpeed: 0.75 | 1;
  romanizationHintsEnabled: boolean;
}
export interface ProgressState {
  completedLessons: string[];
  lastActiveLesson: string | null;
}
```

From src/store/settings.ts:
```typescript
// Zustand store with persist — use useSettingsStore((s) => s.audioSpeed)
```

From src/store/progress.ts:
```typescript
// Zustand store with persist — use useProgressStore((s) => s.completedLessons)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TTS worker, audio utility, and lesson unlock logic</name>
  <files>tts-worker/worker.js, tts-worker/wrangler.toml, .env.local, src/utils/audio.ts, src/utils/audio.test.ts, src/utils/lessonUnlock.ts, src/utils/lessonUnlock.test.ts</files>
  <behavior>
    - speak() with speed 0.75 maps to Naver speed param -2
    - speak() with speed 1.0 maps to Naver speed param 0
    - speak() stops current audio before starting new playback
    - speak() caches blob URLs by text+speed key — second call with same args does not fetch
    - stopAudio() pauses and resets current audio
    - isLessonUnlocked(): first lesson (by level ASC, order ASC) is always unlocked
    - isLessonUnlocked(): lesson N+1 is unlocked only if lesson N is in completedLessons
    - isLessonUnlocked(): Level 2 Lesson 1 unlocks when last lesson of Level 1 is completed
    - isLessonUnlocked(): empty completedLessons means only first lesson is unlocked
  </behavior>
  <action>
    1. Copy `C:/dev/Hanguller/tts-worker/worker.js` and `C:/dev/Hanguller/tts-worker/wrangler.toml` into `tts-worker/` at project root. Update `wrangler.toml` to rename the worker to `hanbuddy-tts`.

    2. Create `.env.local` with `VITE_TTS_WORKER_URL=https://hanguller-tts.marcusschimizzi.workers.dev` (user can update after deploying their own worker).

    3. Create `src/utils/audio.ts` adapted from Hanguller's `C:/dev/Hanguller/src/utils/audio.js`:
       - Export `speak(text: string, speed: 0.75 | 1 = 1): Promise<void>` — fetches from `VITE_TTS_WORKER_URL`, caches blob URLs in a module-level Map keyed by `text|naverSpeed`, stops any currently playing audio before starting. Speed mapping: 0.75 -> -2, 1.0 -> 0. Speaker param: `kyuri`.
       - Export `stopAudio(): void` — pauses and resets current audio.
       - Use `import.meta.env.VITE_TTS_WORKER_URL` for the worker base URL.

    4. Create `src/utils/audio.test.ts` — test the speed mapping logic as a pure function. Extract the speed mapping into a testable helper (`naverSpeed()`). Mock fetch and Audio for speak/stop tests.

    5. Create `src/utils/lessonUnlock.ts`:
       - Export `isLessonUnlocked(lesson: Lesson, allLessons: Lesson[], completedLessons: string[]): boolean`
       - Sort allLessons by (level ASC, order ASC). First in sorted list is always unlocked. Others require previous lesson's ID in completedLessons.

    6. Create `src/utils/lessonUnlock.test.ts` — test all behavior cases listed above.
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npx vitest run src/utils/audio.test.ts src/utils/lessonUnlock.test.ts --reporter=verbose</automated>
  </verify>
  <done>speak() and stopAudio() exported with caching and cancellation, isLessonUnlocked() returns correct unlock state for all edge cases, TTS worker files present in tts-worker/, .env.local created</done>
</task>

<task type="auto">
  <name>Task 2: Router shell, layout, bottom tab bar, and shared UI components</name>
  <files>src/main.tsx, src/App.tsx, src/components/layout/RootLayout.tsx, src/components/layout/BottomTabBar.tsx, src/components/ui/AudioButton.tsx, src/components/ui/SpeedToggle.tsx, src/components/ui/SpeechBadge.tsx, src/components/ui/WelcomeCard.tsx, src/components/ui/CompletionCard.tsx, src/components/ui/Toast.tsx, src/utils/lucideIcons.ts, src/pages/LessonListPage.tsx, src/pages/TopicListPage.tsx, src/pages/SettingsPage.tsx</files>
  <action>
    1. Create `src/utils/lucideIcons.ts` — a string-to-component icon map using explicit Lucide imports (Hand, Hash, Utensils, MapPin, Sun, Users, BookOpen, LayoutGrid, Settings, MessageCircle, Calculator). Export `getIcon(name: string | undefined): LucideIcon` with BookOpen as fallback.

    2. Create `src/components/layout/BottomTabBar.tsx` — fixed bottom nav with three NavLink tabs: Lessons (`/`, end prop), Topics (`/topics`), Settings (`/settings`). Use Lucide icons BookOpen, LayoutGrid, Settings. Style with Tailwind: fixed bottom-0 inset-x-0, bg-white, border-t, flex, each tab is flex-1 centered column with icon + label. Active state: text-blue-600, inactive: text-gray-500.

    3. Create `src/components/layout/RootLayout.tsx` — flex column min-h-screen, main area flex-1 overflow-y-auto with pb-16 (bottom tab height), renders `<Outlet />` above `<BottomTabBar />`.

    4. Create shared UI components:
       - `AudioButton.tsx`: Takes `text: string` prop, reads `audioSpeed` from `useSettingsStore`, calls `speak(text, audioSpeed)` on click. Renders Lucide `Volume2` icon button. Shows loading spinner state during fetch. Prevent double-click with disabled state while playing.
       - `SpeedToggle.tsx`: Reads/writes `audioSpeed` via `useSettingsStore`. Renders two buttons: "0.75x" and "1x" with active state highlighting. Compact inline design for lesson page header.
       - `SpeechBadge.tsx`: Takes `level: SpeechLevel` prop. Renders colored badge — formal-high: blue-100/blue-800 "합쇼체", polite: green-100/green-800 "해요체", informal: yellow-100/yellow-800 "반말", plain: gray-100/gray-600 "기본형". Small text-xs rounded pill.
       - `WelcomeCard.tsx`: Reads `completedLessons` from `useProgressStore`. Shows when `completedLessons.length === 0`. Renders card: "Welcome to Hanbuddy! Start your Korean journey with Lesson 1" + "Start Learning" button that navigates to `/lessons/les-001` via `useNavigate`. Styled as a highlighted card with bg-gradient.
       - `CompletionCard.tsx`: Takes `lessonTitle: string`, `nextLessonId: string | null`, `nextLessonTitle: string | null`, `onContinue: () => void` props. Uses framer-motion AnimatePresence with scale+opacity entrance. Shows "Lesson Complete!" + next lesson info + Continue button. If no next lesson, shows "All lessons complete!" message.
       - `Toast.tsx`: Takes `message: string | null` and `onDismiss: () => void`. Uses framer-motion AnimatePresence with slide-up from bottom. Fixed positioned above the bottom tab bar. Auto-dismisses after 2500ms via useEffect.

    5. Create stub pages (enough to verify routing):
       - `LessonListPage.tsx`: Renders `<h1>Lessons</h1>` placeholder + `<WelcomeCard />`. Will be fully built in Plan 03.
       - `TopicListPage.tsx`: Renders `<h1>Topics</h1>` placeholder. Will be fully built in Plan 04.
       - `SettingsPage.tsx`: Renders `<h1>Settings</h1>` placeholder. Will be fully built in Plan 04.

    6. Update `src/main.tsx` — replace current render with `createBrowserRouter` + `RouterProvider`. Define routes:
       - `{ path: '/', element: <RootLayout />, children: [...] }`
       - Index route: `<LessonListPage />`
       - `lessons/:lessonId`: placeholder (Plan 03 creates LessonDetailPage)
       - `topics`: `<TopicListPage />`
       - `topics/:topicId`: placeholder (Plan 04 creates TopicDetailPage)
       - `settings`: `<SettingsPage />`

    7. Update `src/App.tsx` — can be emptied or removed since main.tsx now uses RouterProvider directly. If keeping, make it a simple pass-through.
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>App boots with bottom tab navigation between three tabs, WelcomeCard shows on fresh state, all shared UI components exist and are importable, routing works for all defined paths</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for SpeechBadge and WelcomeCard (Wave 0 — GRAM-05, PROG-05)</name>
  <files>src/components/ui/SpeechBadge.test.tsx, src/components/ui/WelcomeCard.test.tsx</files>
  <behavior>
    - SpeechBadge renders correct Korean label for each speech level: formal-high -> "합쇼체", polite -> "해요체", informal -> "반말", plain -> "기본형"
    - SpeechBadge applies correct color class for each speech level (blue for formal-high, green for polite, yellow for informal, gray for plain)
    - WelcomeCard renders when completedLessons is empty
    - WelcomeCard does NOT render when completedLessons has at least one entry
    - WelcomeCard "Start Learning" button links to /lessons/les-001
  </behavior>
  <action>
    1. Create `src/components/ui/SpeechBadge.test.tsx`:
       - Import SpeechBadge and render with each SpeechLevel value
       - Assert correct Korean label text is present for each level
       - Assert correct Tailwind color classes are applied (check className or use snapshot)
       - Use @testing-library/react render + screen queries

    2. Create `src/components/ui/WelcomeCard.test.tsx`:
       - Mock `useProgressStore` to control `completedLessons` state
       - Mock `useNavigate` from react-router-dom
       - Test: when completedLessons is [], WelcomeCard renders welcome text and "Start Learning" button
       - Test: when completedLessons has entries, WelcomeCard returns null (nothing rendered)
       - Test: "Start Learning" button navigates to `/lessons/les-001`
       - Wrap renders in MemoryRouter for Link/NavLink support if needed
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npx vitest run src/components/ui/SpeechBadge.test.tsx src/components/ui/WelcomeCard.test.tsx --reporter=verbose</automated>
  </verify>
  <done>SpeechBadge test verifies all four speech level labels and colors, WelcomeCard test verifies conditional rendering and navigation — all passing</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all existing + new tests pass
- `npm run build` — production build succeeds
- `npx tsc --noEmit` — no TypeScript errors
- App loads at localhost, bottom tab bar visible, tabs navigate correctly
</verification>

<success_criteria>
- Audio utility with speak/stop/cache exported and tested
- Lesson unlock logic exported and tested with edge cases
- TTS worker files present in tts-worker/
- Router with three tabs (Lessons/Topics/Settings) working
- All shared UI components (AudioButton, SpeedToggle, SpeechBadge, WelcomeCard, CompletionCard, Toast) created and importable
- Lucide icon map for dynamic topic icons
- WelcomeCard visible on Lessons tab with no completed lessons
- SpeechBadge and WelcomeCard have unit tests verifying behavioral correctness
</success_criteria>

<output>
After completion, create `.planning/phases/02-lessons/02-01-SUMMARY.md`
</output>
