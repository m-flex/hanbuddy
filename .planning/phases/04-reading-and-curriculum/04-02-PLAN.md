---
phase: 04-reading-and-curriculum
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/BottomTabBar.tsx
  - src/main.tsx
  - src/pages/CurriculumMapPage.tsx
  - src/pages/CurriculumMapPage.test.tsx
  - src/components/curriculum/CurriculumNode.tsx
  - src/components/curriculum/CurriculumPath.tsx
autonomous: true
requirements:
  - PROG-03

must_haves:
  truths:
    - "User sees a vertical path of lesson nodes showing completed (checkmark), current (play), and locked (lock) states"
    - "Tapping a completed or current node navigates to the lesson detail page"
    - "Tapping a locked node shows a toast explaining what needs to be completed"
    - "The Learn tab is the primary tab in the bottom bar, replacing Lessons"
    - "A 5th Read tab exists in the bottom bar between Practice and Topics"
  artifacts:
    - path: "src/pages/CurriculumMapPage.tsx"
      provides: "Curriculum map view as Learn tab index"
      min_lines: 40
    - path: "src/components/curriculum/CurriculumNode.tsx"
      provides: "Single lesson node with state-based icon and styling"
      exports: ["CurriculumNode"]
    - path: "src/components/curriculum/CurriculumPath.tsx"
      provides: "Vertical path container connecting nodes"
      exports: ["CurriculumPath"]
    - path: "src/components/layout/BottomTabBar.tsx"
      provides: "5-tab navigation with Learn and Read tabs"
      contains: "Read"
    - path: "src/main.tsx"
      provides: "Routes for /read and CurriculumMapPage at index"
      contains: "CurriculumMapPage"
  key_links:
    - from: "src/pages/CurriculumMapPage.tsx"
      to: "src/store/progress.ts"
      via: "useProgressStore for completedLessons"
      pattern: "useProgressStore"
    - from: "src/pages/CurriculumMapPage.tsx"
      to: "src/utils/lessonUnlock.ts"
      via: "isLessonUnlocked for node state"
      pattern: "isLessonUnlocked"
    - from: "src/main.tsx"
      to: "src/pages/CurriculumMapPage.tsx"
      via: "index route element"
      pattern: "CurriculumMapPage"
---

<objective>
Build the curriculum map page and update navigation to 5-tab layout with Learn and Read tabs.

Purpose: Users see their learning journey as a visual vertical path, replacing the flat lesson list. The navigation restructuring adds the Read tab for Phase 4's reading passages (page content built in Plan 03).
Output: CurriculumMapPage with node components, 5-tab BottomTabBar, updated router.
</objective>

<execution_context>
@C:/Users/Marcu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Marcu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reading-and-curriculum/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/utils/lessonUnlock.ts:
```typescript
export function isLessonUnlocked(lesson: Lesson, allLessons: Lesson[], completedLessons: string[]): boolean;
```

From src/store/progress.ts:
```typescript
// useProgressStore provides:
// completedLessons: string[]
// lastActiveLesson: string | null
```

From src/data/lessons.ts:
```typescript
export const LESSONS = [...] satisfies Lesson[];
// 5 lessons sorted by level/order: les-001..les-005
```

From src/components/layout/BottomTabBar.tsx (current 4-tab):
```typescript
// NavLink tabs: / (Lessons), /practice, /topics, /settings
// Icons: BookOpen, Dumbbell, LayoutGrid, Settings
```

From src/main.tsx (current router):
```typescript
// { index: true, element: <LessonListPage /> }
// Routes: /, lessons/:lessonId, topics, topics/:topicId, settings, practice, practice/*
```

From src/components/ui/Toast.tsx:
```typescript
// Toast component with framer-motion. Used in LessonListPage for locked lesson feedback.
```

From src/components/ui/WelcomeCard.tsx:
```typescript
// Shows for new users with no completed lessons. Button navigates to /lessons/les-001.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curriculum map components and page</name>
  <files>src/components/curriculum/CurriculumNode.tsx, src/components/curriculum/CurriculumPath.tsx, src/pages/CurriculumMapPage.tsx, src/pages/CurriculumMapPage.test.tsx</files>
  <behavior>
    - CurriculumMapPage renders one node per lesson in level/order sequence
    - Completed lessons show a checkmark icon
    - The first unlocked-but-incomplete lesson shows a play icon (current)
    - Locked lessons show a lock icon
    - Tapping a completed/current node navigates to /lessons/{lessonId}
    - Tapping a locked node shows a toast (not navigation)
    - When no lessons are completed, les-001 is current (play icon), rest locked
    - Auto-scrolls to current node on mount
  </behavior>
  <action>
    1. Create src/components/curriculum/CurriculumNode.tsx:
       - Props: `{ lesson: Lesson; state: 'completed' | 'current' | 'locked'; isLast: boolean; onTap: () => void; }`
       - Layout: relative flex with vertical line (w-0.5 bg-gray-200 absolute from circle center to bottom, hidden on last node)
       - Circle: w-8 h-8 rounded-full with state-based colors:
         - completed: bg-green-500 text-white with CheckCircle2 icon (size 16)
         - current: bg-blue-500 text-white with Play icon (size 14)
         - locked: bg-gray-200 text-gray-400 with Lock icon (size 14)
       - Label: lesson.title, text-sm font-medium, gray-400 when locked, gray-900 otherwise
       - Level header: render a level label (e.g. "Level 1") above the first node of each level group

    2. Create src/components/curriculum/CurriculumPath.tsx:
       - Props: `{ children: React.ReactNode }` — simple vertical container
       - Layout: `flex flex-col` with padding

    3. Create src/pages/CurriculumMapPage.tsx:
       - Import LESSONS, useProgressStore, isLessonUnlocked, Toast, WelcomeCard
       - Sort LESSONS by level ASC, order ASC
       - Derive currentLessonId: first lesson where isLessonUnlocked=true AND not in completedLessons
       - getNodeState(lesson): completed | current | locked
       - On completed/current node tap: navigate(`/lessons/${lesson.id}`)
       - On locked node tap: show toast "Complete previous lessons to unlock"
       - Show WelcomeCard at top when completedLessons is empty (same logic as current LessonListPage)
       - Auto-scroll to current node using useRef + useEffect with scrollIntoView({ behavior: 'smooth', block: 'center' })
       - Page header: "Your Learning Path" or similar

    4. Write tests FIRST in src/pages/CurriculumMapPage.test.tsx:
       - Mock useProgressStore and LESSONS
       - Test: renders completed node with checkmark for completed lesson
       - Test: renders current node with play icon for first unlocked lesson
       - Test: renders locked node with lock icon for locked lessons
       - Test: shows toast on locked node tap
       Use vi.mock for stores, MemoryRouter wrapper, render from @testing-library/react.
  </action>
  <verify>
    <automated>npx vitest run src/pages/CurriculumMapPage.test.tsx --reporter=verbose</automated>
  </verify>
  <done>CurriculumMapPage renders a vertical path with correct node states, navigation works for unlocked nodes, locked nodes show toast, tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update BottomTabBar to 5 tabs and wire router</name>
  <files>src/components/layout/BottomTabBar.tsx, src/main.tsx</files>
  <action>
    1. Modify src/components/layout/BottomTabBar.tsx:
       - Replace 4-tab layout with 5 tabs: Learn | Practice | Read | Topics | Settings
       - Import additional icons: GraduationCap (Learn), BookText (Read) from lucide-react
       - Tab order and routes:
         - Learn: to="/" end, icon=GraduationCap, label="Learn"
         - Practice: to="/practice", icon=Dumbbell, label="Practice"
         - Read: to="/read", icon=BookText, label="Read"
         - Topics: to="/topics", icon=LayoutGrid, label="Topics"
         - Settings: to="/settings", icon=Settings, label="Settings"
       - Remove BookOpen import (was used for old Lessons tab)
       - Use text-[10px] instead of text-xs for tab labels to prevent wrapping at 320px viewport
       - Reduce icon size from 20 to 18 for the 5-tab layout

    2. Modify src/main.tsx:
       - Replace LessonListPage import with CurriculumMapPage import
       - Change index route: `{ index: true, element: <CurriculumMapPage /> }`
       - Add placeholder routes for reading pages (will be implemented in Plan 03):
         - `{ path: 'read', element: <ReadingListPage /> }`
         - `{ path: 'read/:passageId', element: <ReadingPassagePage /> }`
       - Create minimal stub pages for ReadingListPage and ReadingPassagePage:
         - src/pages/ReadingListPage.tsx: `export default function ReadingListPage() { return <div className="p-4"><h1 className="text-xl font-bold">Reading</h1><p className="text-gray-500 mt-2">Coming soon...</p></div>; }`
         - src/pages/ReadingPassagePage.tsx: `export default function ReadingPassagePage() { return <div className="p-4">Passage loading...</div>; }`
       - Keep LessonListPage import and its old route REMOVED — curriculum map replaces it. The /lessons/:lessonId route stays.

    3. Verify the app builds with no type errors: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>BottomTabBar shows 5 tabs (Learn, Practice, Read, Topics, Settings). Index route renders CurriculumMapPage. /read and /read/:passageId routes exist (stubs). All existing tests pass. App builds clean.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full test suite passes
- `npx tsc --noEmit` — no type errors
- BottomTabBar has exactly 5 NavLink children
- Index route renders CurriculumMapPage (not LessonListPage)
- /read route exists in router
</verification>

<success_criteria>
- Curriculum map shows lessons as vertical path with correct completed/current/locked states
- Tapping unlocked nodes navigates, locked nodes show toast
- 5-tab BottomTabBar with Learn and Read tabs renders correctly
- Router updated with CurriculumMapPage at index and /read routes
- All tests pass, app builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-reading-and-curriculum/04-02-SUMMARY.md`
</output>
