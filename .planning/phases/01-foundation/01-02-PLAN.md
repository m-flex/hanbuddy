---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/store/progress.ts
  - src/store/settings.ts
  - src/engine/korean.ts
  - src/hooks/useRomanizationToggle.ts
  - src/store/progress.test.ts
  - src/engine/korean.test.ts
  - src/hooks/useRomanizationToggle.test.ts
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "App boot reads zero saved state from localStorage and writes an initial versioned schema record without error"
    - "A simulated schema migration (version bump) runs on boot and correctly transforms old shape to new shape without data loss"
    - "Korean particle selection via es-hangul produces correct 은/는 and 이/가 forms for both consonant-final and vowel-final nouns"
    - "Romanization is rendered only when explicitly revealed — it does not appear in any default view"
    - "Corrupted localStorage data triggers reset to fresh state with corruption flag set"
  artifacts:
    - path: "src/store/progress.ts"
      provides: "Progress Zustand store with persist middleware, versioned migration, corruption handling"
      exports: ["useProgressStore"]
    - path: "src/store/settings.ts"
      provides: "Settings Zustand store with persist middleware for audio and romanization preferences"
      exports: ["useSettingsStore"]
    - path: "src/engine/korean.ts"
      provides: "Korean text processing utilities wrapping es-hangul"
      exports: ["attachJosa", "wordHasBatchim", "decompose"]
    - path: "src/hooks/useRomanizationToggle.ts"
      provides: "Per-word romanization reveal toggle hook"
      exports: ["useRomanizationToggle"]
  key_links:
    - from: "src/store/progress.ts"
      to: "zustand/middleware persist"
      via: "persist middleware with version + migrate"
      pattern: "persist\\("
    - from: "src/store/progress.ts"
      to: "localStorage"
      via: "createJSONStorage(() => localStorage)"
      pattern: "createJSONStorage"
    - from: "src/engine/korean.ts"
      to: "es-hangul"
      via: "import { josa, hasBatchim, disassemble }"
      pattern: "from 'es-hangul'"
    - from: "src/hooks/useRomanizationToggle.ts"
      to: "React state"
      via: "useState for per-word reveal tracking"
      pattern: "useState<string \\| null>"
---

<objective>
Implement the storage layer, Korean text engine, and romanization toggle to complete all Phase 1 foundation requirements.

Purpose: Zustand stores with versioned migrations ensure data survives schema changes. The es-hangul wrapper provides correct Korean particle selection. The romanization hook enforces the "hidden by default, reveal on tap" principle. Together these three utilities are consumed by every feature in Phases 2-4.

Output: Working Zustand stores (progress, settings) with persist + migration, Korean engine utilities, romanization toggle hook, and comprehensive unit tests for all four FOUND requirements.
</objective>

<execution_context>
@C:/Users/Marcu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Marcu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types the executor needs -->

From src/types/content.ts (created in Plan 01):
```typescript
export type SpeechLevel = 'formal-high' | 'polite' | 'informal' | 'plain';
export type ConjugationType = 'action' | 'descriptive' | 'noun' | 'particle' | 'expression';
export interface VocabItem {
  id: `voc-${number}`;
  korean: string;
  english: string;
  romanization: string;
  speech_level: SpeechLevel;
  conjugation_type: ConjugationType;
  topics: `top-${number}`[];
  examples?: ExampleSentence[];
}
```

From src/types/store.ts (created in Plan 01):
```typescript
export interface ProgressState {
  completedLessons: string[];
  lastActiveLesson: string | null;
}
export interface SettingsState {
  audioEnabled: boolean;
  audioSpeed: 0.75 | 1;
  romanizationHintsEnabled: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zustand stores with persist middleware and versioned migration</name>
  <files>src/store/progress.ts, src/store/settings.ts</files>
  <behavior>
    - useProgressStore initializes with empty completedLessons array and null lastActiveLesson
    - useProgressStore persists to localStorage key 'hanbuddy_progress' with version: 1
    - useProgressStore migrate function handles version 0 (fresh/legacy) state by returning defaults
    - useProgressStore onRehydrateStorage sets _corruptionDetected flag on parse error
    - useSettingsStore initializes with audioEnabled: true, audioSpeed: 1, romanizationHintsEnabled: true
    - useSettingsStore persists to localStorage key 'hanbuddy_settings' with version: 1
    - Both stores use Zustand v5 curried syntax: create<State>()(persist(...))
  </behavior>
  <action>
    1. Create src/store/progress.ts:
       - Import `create` from 'zustand', `persist` and `createJSONStorage` from 'zustand/middleware'
       - Define the full ProgressState interface (extending the one from types/store.ts with actions):
         ```typescript
         interface ProgressStoreState {
           completedLessons: string[];
           lastActiveLesson: string | null;
           _corruptionDetected: boolean;
           completeLesson: (lessonId: string) => void;
           setActiveLesson: (lessonId: string) => void;
           clearCorruptionFlag: () => void;
         }
         ```
       - Use `create<ProgressStoreState>()(persist(...))` — note the double-parenthesis (Zustand v5 curried syntax, per research Pitfall 1)
       - Set `name: 'hanbuddy_progress'` (prefix convention per research Open Question 1 — separate keys, not one monolithic key)
       - Set `version: 1` explicitly (per research Pitfall 4 — never rely on default 0)
       - Set `storage: createJSONStorage(() => localStorage)`
       - Implement `migrate` function:
         - If `storedVersion < 1`: return default state (treat as fresh — per Pitfall 4)
         - Future migrations chain: `if (storedVersion < 2) { ... }` etc.
       - Implement `onRehydrateStorage` corruption handler:
         - On error: `console.warn('hanbuddy_progress corrupted, resetting')`, remove key from localStorage, set `_corruptionDetected: true` in store (per Pitfall 6 — don't show toast here, set flag for component useEffect)
       - Implement `partialize` to exclude `_corruptionDetected` and action functions from persistence

    2. Create src/store/settings.ts:
       - Same persist pattern as progress store
       - `name: 'hanbuddy_settings'`, `version: 1`
       - State:
         ```typescript
         interface SettingsStoreState {
           audioEnabled: boolean;
           audioSpeed: 0.75 | 1;
           romanizationHintsEnabled: boolean;
           setAudioEnabled: (v: boolean) => void;
           setAudioSpeed: (v: 0.75 | 1) => void;
           setRomanizationHintsEnabled: (v: boolean) => void;
         }
         ```
       - Defaults: `audioEnabled: true`, `audioSpeed: 1`, `romanizationHintsEnabled: true`
       - Migrate: same defensive pattern (version < 1 returns defaults)
       - Partialize: exclude action functions from persistence
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Both stores compile. Progress store persists to 'hanbuddy_progress' with version 1, migration, and corruption handling. Settings store persists to 'hanbuddy_settings' with version 1. Both use Zustand v5 curried syntax.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Korean engine utilities and romanization toggle hook</name>
  <files>src/engine/korean.ts, src/hooks/useRomanizationToggle.ts</files>
  <behavior>
    - attachJosa('물', '이/가') returns '물이'
    - attachJosa('나라', '이/가') returns '나라가'
    - attachJosa('선생님', '은/는') returns '선생님은'
    - attachJosa('의사', '은/는') returns '의사는'
    - attachJosa('물', '을/를') returns '물을'
    - attachJosa('사과', '을/를') returns '사과를'
    - wordHasBatchim('물') returns true (ㄹ final consonant)
    - wordHasBatchim('나라') returns false (vowel final)
    - useRomanizationToggle: initially no word is revealed
    - useRomanizationToggle: toggle('voc-001') reveals voc-001
    - useRomanizationToggle: toggle('voc-001') again hides voc-001
    - useRomanizationToggle: toggle('voc-002') while voc-001 revealed hides voc-001 and reveals voc-002
    - useRomanizationToggle: hideAll() hides everything
  </behavior>
  <action>
    1. Create src/engine/korean.ts:
       - Import `josa`, `hasBatchim`, `disassemble` from 'es-hangul'
       - Define `JosaType` union: `'이/가' | '을/를' | '은/는' | '으로/로' | '와/과' | '아/야'`
       - Export `attachJosa(word: string, particle: JosaType): string` — thin wrapper around `josa(word, particle)`
       - Export `wordHasBatchim(word: string): boolean` — wrapper around `hasBatchim(word)`
       - Export `decompose(text: string): string` — wrapper around `disassemble(text)` (returns string per es-hangul API)
       - Keep it thin — the value is typed particle selection and centralized import, not added logic

    2. Create src/hooks/useRomanizationToggle.ts:
       - Import `useState`, `useCallback` from 'react'
       - Track `revealedWordId: string | null` (only one word revealed at a time — per locked decision "tap/click per-word inline reveal")
       - `toggle(wordId: string)`: if currently revealed word matches, hide it (set null); otherwise reveal new word (set wordId). This implements "stays visible until user taps the word again or taps elsewhere"
       - `isRevealed(wordId: string): boolean`: returns `revealedWordId === wordId`
       - `hideAll()`: sets `revealedWordId` to null (for "taps elsewhere" dismissal)
       - Return `{ toggle, isRevealed, hideAll }` from hook
       - Default state is null (nothing revealed) — this satisfies FOUND-04 "never primary display"
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Korean engine wraps es-hangul with typed particle selection. Romanization hook implements per-word toggle with hidden-by-default behavior. Both compile cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for stores, engine, and romanization hook</name>
  <files>src/store/progress.test.ts, src/engine/korean.test.ts, src/hooks/useRomanizationToggle.test.ts</files>
  <behavior>
    - Progress store: initializes with empty state, persists to localStorage, migration transforms old shape
    - Settings store: initializes with defaults, persists to localStorage
    - Korean engine: correct josa attachment for 은/는 and 이/가 with both consonant and vowel finals
    - Romanization hook: hidden by default, toggle reveals/hides, hideAll clears
  </behavior>
  <action>
    1. Create src/engine/korean.test.ts:
       - Test `attachJosa` with all particle types from the behavior list:
         - `'이/가'` with consonant-final (물 → 물이) and vowel-final (나라 → 나라가)
         - `'은/는'` with consonant-final (선생님 → 선생님은) and vowel-final (의사 → 의사는)
         - `'을/를'` with consonant-final (물 → 물을) and vowel-final (사과 → 사과를)
         - `'으로/로'` with consonant-final (바깥 → 바깥으로), ㄹ-final (서울 → 서울로), vowel-final (내부 → 내부로)
       - Test `wordHasBatchim`:
         - `'물'` → true, `'나라'` → false, `'선생님'` → true, `'의사'` → false
       - These tests directly validate FOUND-03 success criteria (은/는 and 이/가 for both consonant-final and vowel-final)

    2. Create src/store/progress.test.ts:
       - Before each test: clear localStorage, reset store to initial state using `useProgressStore.setState` or by creating a fresh store
       - Test initial state: `completedLessons` is empty array, `lastActiveLesson` is null
       - Test `completeLesson`: adds lesson ID to completedLessons
       - Test `setActiveLesson`: sets lastActiveLesson
       - Test persist: after setting state, check `localStorage.getItem('hanbuddy_progress')` contains the data
       - Test migration: manually set `localStorage.setItem('hanbuddy_progress', JSON.stringify({ state: { lessonsDone: ['les-001'] }, version: 0 }))`, create store, verify migration produces valid state with defaults
       - Test corruption: set `localStorage.setItem('hanbuddy_progress', 'not-json{{{')` — verify store initializes with defaults and `_corruptionDetected` is set
       - These tests directly validate FOUND-02 success criteria (boot reads/writes, migration transforms)

    3. Create src/hooks/useRomanizationToggle.test.ts:
       - Use `@testing-library/react` `renderHook` for hook testing
       - Test default state: `isRevealed('any-id')` returns false (FOUND-04: hidden by default)
       - Test toggle reveal: call `toggle('voc-001')`, then `isRevealed('voc-001')` returns true
       - Test toggle hide: call `toggle('voc-001')` twice, `isRevealed('voc-001')` returns false
       - Test exclusive reveal: reveal voc-001, then toggle voc-002 — voc-001 hidden, voc-002 revealed
       - Test hideAll: reveal voc-001, call `hideAll()`, `isRevealed('voc-001')` returns false
       - These tests validate FOUND-04: romanization never shown by default, only on explicit reveal

    Note: For Zustand store tests, you may need to mock localStorage or use the store's vanilla API. Use `useProgressStore.getState()` and `useProgressStore.setState()` for non-React test access.
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npx vitest run --reporter=verbose 2>&1</automated>
  </verify>
  <done>All unit tests pass. Korean particle selection verified for consonant-final and vowel-final nouns. Store persist and migration verified. Romanization hidden-by-default verified. Full test suite runs green alongside Plan 01's content tests.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` — all tests pass (content, store, engine, hook)
2. `npx tsc --noEmit` — zero TypeScript errors across entire project
3. `npm run build` — production build succeeds
4. Korean particle test cases match ROADMAP success criterion #4: "Korean particle selection via es-hangul produces correct 은/는 and 이/가 forms for both consonant-final and vowel-final nouns"
5. Romanization test confirms hidden-by-default matching ROADMAP success criterion #5
</verification>

<success_criteria>
- Zustand progress store persists to 'hanbuddy_progress' and settings store to 'hanbuddy_settings'
- Schema migration from version 0 to version 1 produces valid default state
- Corrupted localStorage triggers reset with _corruptionDetected flag
- attachJosa('물', '이/가') returns '물이' and attachJosa('나라', '이/가') returns '나라가'
- attachJosa('선생님', '은/는') returns '선생님은' and attachJosa('의사', '은/는') returns '의사는'
- Romanization toggle defaults to hidden, reveals on toggle, hides on second toggle
- All vitest tests pass, tsc --noEmit clean, npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
