---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - eslint.config.js
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/types/content.ts
  - src/types/store.ts
  - src/data/vocab.ts
  - src/data/grammar.ts
  - src/data/lessons.ts
  - src/data/topics.ts
  - src/types/content.test.ts
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "A TypeScript VocabItem with speech_level, conjugation_type, and voc-NNN ID compiles without error"
    - "A malformed VocabItem (missing field or wrong type) causes a TypeScript build error"
    - "All content IDs follow the prefixed counter pattern (voc-NNN, grm-NNN, les-NNN, top-NNN)"
    - "The project builds and dev server starts without errors"
  artifacts:
    - path: "src/types/content.ts"
      provides: "VocabItem, GrammarPoint, Lesson, Topic, ExampleSentence interfaces and SpeechLevel, ConjugationType union types"
      exports: ["VocabItem", "GrammarPoint", "Lesson", "Topic", "ExampleSentence", "SpeechLevel", "ConjugationType"]
    - path: "src/data/vocab.ts"
      provides: "Sample vocabulary data validated with satisfies"
      exports: ["VOCAB"]
    - path: "src/data/grammar.ts"
      provides: "Sample grammar data validated with satisfies"
      exports: ["GRAMMAR"]
    - path: "src/data/lessons.ts"
      provides: "Sample lesson data referencing vocab and grammar IDs"
      exports: ["LESSONS"]
    - path: "src/data/topics.ts"
      provides: "Topic definitions"
      exports: ["TOPICS"]
  key_links:
    - from: "src/data/vocab.ts"
      to: "src/types/content.ts"
      via: "import type + satisfies operator"
      pattern: "satisfies VocabItem\\[\\]"
    - from: "src/data/grammar.ts"
      to: "src/types/content.ts"
      via: "import type + satisfies operator"
      pattern: "satisfies GrammarPoint\\[\\]"
    - from: "src/data/lessons.ts"
      to: "src/types/content.ts"
      via: "import type + satisfies operator"
      pattern: "satisfies Lesson\\[\\]"
---

<objective>
Scaffold the Hanbuddy project and establish the content data schema with type-safe sample data.

Purpose: Create the foundational project structure and type system that every subsequent phase builds on. The TypeScript content schema enforces correctness at build time — malformed content is a compiler error, not a runtime bug.

Output: A running Vite + React + TypeScript project with content type definitions, sample data files for vocab/grammar/lessons/topics, and passing type validation tests.
</objective>

<execution_context>
@C:/Users/Marcu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Marcu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project with all dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, eslint.config.js, index.html, src/main.tsx, src/App.tsx, src/App.css, src/index.css, src/vite-env.d.ts</files>
  <action>
    1. Run `npm create vite@latest . -- --template react-ts` from the project root (C:/dev/Hanbuddy). Since .planning/ and .git/ already exist, use the current directory. If Vite prompts about existing files, allow it to proceed — it will not overwrite .planning/ or .git/.

    2. Install production dependencies:
       `npm install zustand es-hangul framer-motion lucide-react react-router-dom`

    3. Install dev dependencies:
       `npm install -D tailwindcss @tailwindcss/vite vitest @testing-library/react @testing-library/jest-dom jsdom`

    4. Configure vite.config.ts — add Tailwind v4 plugin and Vitest config:
       ```typescript
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';
       import tailwindcss from '@tailwindcss/vite';

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         test: {
           globals: true,
           environment: 'jsdom',
           setupFiles: './src/test-setup.ts',
         },
       });
       ```

    5. Replace src/index.css content with Tailwind v4 import:
       ```css
       @import "tailwindcss";
       ```
       Do NOT use old `@tailwind base/components/utilities` directives — they do not work in Tailwind v4.

    6. Create src/test-setup.ts with:
       ```typescript
       import '@testing-library/jest-dom';
       ```

    7. Create the folder structure (empty directories with .gitkeep or index.ts barrel files):
       - src/types/
       - src/data/
       - src/engine/
       - src/store/
       - src/hooks/
       - src/components/
       - src/pages/
       - src/utils/
       - src/assets/

    8. Replace src/App.tsx with a minimal placeholder that confirms the app renders:
       ```tsx
       function App() {
         return (
           <div className="min-h-screen bg-white flex items-center justify-center">
             <h1 className="text-2xl font-bold text-gray-900">Hanbuddy</h1>
           </div>
         );
       }
       export default App;
       ```

    9. Verify dev server starts: `npm run dev` (confirm no errors in output, then stop).
    10. Verify build succeeds: `npm run build` (confirm no TypeScript or build errors).
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Project scaffolded with all dependencies installed. Dev server starts. Build succeeds. Folder structure matches the locked decision (components/, pages/, store/, data/, utils/, hooks/, types/, engine/).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define content type system and author sample data</name>
  <files>src/types/content.ts, src/types/store.ts, src/data/vocab.ts, src/data/grammar.ts, src/data/lessons.ts, src/data/topics.ts</files>
  <behavior>
    - VocabItem with all required fields (id as voc-NNN, korean, english, romanization, speech_level, conjugation_type, topics) compiles
    - VocabItem missing 'english' field causes build error via satisfies
    - GrammarPoint with id as grm-NNN, title, explanation, speech_level, conjugation_type, pattern, examples compiles
    - Lesson with id as les-NNN referencing vocab and grammar IDs compiles
    - Topic with id as top-NNN and name compiles
    - SpeechLevel only accepts 'formal-high' | 'polite' | 'informal' | 'plain'
    - ConjugationType only accepts 'action' | 'descriptive' | 'noun' | 'particle' | 'expression'
  </behavior>
  <action>
    1. Create src/types/content.ts with these type definitions:

       - `SpeechLevel` union type: `'formal-high' | 'polite' | 'informal' | 'plain'`
       - `ConjugationType` union type: `'action' | 'descriptive' | 'noun' | 'particle' | 'expression'`
       - `ExampleSentence` interface: `{ korean: string; english: string; romanization: string }`
       - `VocabItem` interface with:
         - `id: \`voc-${number}\``  (template literal type for prefix enforcement)
         - `korean: string`
         - `english: string`
         - `romanization: string`  (authored, not computed — per locked decision)
         - `speech_level: SpeechLevel`
         - `conjugation_type: ConjugationType`
         - `topics: \`top-${number}\`[]`  (multi-tag topic system per locked decision)
         - `examples?: ExampleSentence[]`
       - `GrammarPoint` interface with:
         - `id: \`grm-${number}\``
         - `title: string`
         - `explanation: string`
         - `speech_level: SpeechLevel`
         - `conjugation_type: ConjugationType`
         - `pattern: string`  (the grammar pattern, e.g., "N + 은/는")
         - `examples: ExampleSentence[]`
         - `topics: \`top-${number}\`[]`
       - `Lesson` interface with:
         - `id: \`les-${number}\``
         - `title: string`
         - `description: string`
         - `level: number`  (1, 2, 3... for sequential unlock)
         - `order: number`  (position within a level)
         - `vocab_ids: \`voc-${number}\`[]`  (references, not embedded)
         - `grammar_ids: \`grm-${number}\`[]`
         - `topics: \`top-${number}\`[]`
       - `Topic` interface with:
         - `id: \`top-${number}\``
         - `name: string`
         - `description: string`
         - `icon?: string`  (Lucide icon name)

    2. Create src/types/store.ts with store state interfaces (used by Plan 02):
       - `ProgressState`: `{ completedLessons: string[]; lastActiveLesson: string | null; }`
       - `SettingsState`: `{ audioEnabled: boolean; audioSpeed: 0.75 | 1; romanizationHintsEnabled: boolean; }`
       - Export all with actions (set functions) as separate types

    3. Create src/data/topics.ts — define 5-6 starter topics:
       - `top-001` Greetings, `top-002` Numbers, `top-003` Food & Drink, `top-004` Daily Life, `top-005` Travel, `top-006` Family
       - Export as `TOPICS` array using `satisfies Topic[]`

    4. Create src/data/vocab.ts — author 8-10 sample vocabulary items covering:
       - At least 2 speech levels (formal-high and polite)
       - At least 2 conjugation types (noun and expression)
       - Multiple topic tags on some items (e.g., 물 tagged food + daily-life)
       - Each with authored romanization (Revised Romanization standard)
       - At least 3 items with example sentences
       - Export as `VOCAB` array using `satisfies VocabItem[]`

    5. Create src/data/grammar.ts — author 3-4 sample grammar points:
       - Topic particle (은/는), subject particle (이/가), object particle (을/를)
       - Each with 3+ example sentences
       - Export as `GRAMMAR` array using `satisfies GrammarPoint[]`

    6. Create src/data/lessons.ts — author 2 sample lessons:
       - Level 1, Lesson 1: "Greetings" referencing relevant vocab and grammar IDs
       - Level 1, Lesson 2: "Basic Particles" referencing particle grammar points
       - Export as `LESSONS` array using `satisfies Lesson[]`

    All data files use `satisfies` operator (NOT type annotation) so that:
    - TypeScript infers the literal types for autocomplete
    - The compiler rejects missing/wrong fields at build time
    - This directly satisfies FOUND-01 success criterion #1
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>All content types defined and exported. Sample data compiles with satisfies. A deliberately malformed entry (missing field) would cause a build error. IDs follow voc-NNN/grm-NNN/les-NNN/top-NNN convention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Content schema validation tests</name>
  <files>src/types/content.test.ts</files>
  <behavior>
    - VOCAB array has length > 0
    - Every VocabItem has a unique id matching /^voc-\d{3}$/
    - Every VocabItem has non-empty korean, english, romanization
    - Every VocabItem has a valid speech_level value
    - Every VocabItem has a valid conjugation_type value
    - Every VocabItem topics array contains valid top-NNN references that exist in TOPICS
    - GRAMMAR array has length > 0
    - Every GrammarPoint has a unique id matching /^grm-\d{3}$/
    - Every GrammarPoint has at least 1 example sentence
    - LESSONS array has length > 0
    - Every Lesson has a unique id matching /^les-\d{3}$/
    - Every Lesson references only vocab IDs that exist in VOCAB
    - Every Lesson references only grammar IDs that exist in GRAMMAR
    - No duplicate IDs across any collection
  </behavior>
  <action>
    Create src/types/content.test.ts using Vitest:

    1. Import VOCAB, GRAMMAR, LESSONS, TOPICS from data files
    2. Import the type constants (SpeechLevel values, etc.) or hardcode the valid sets

    Test groups:

    **Vocab data integrity:**
    - `VOCAB.length > 0`
    - All IDs unique (new Set check)
    - All IDs match `/^voc-\d{3}$/` (enforces zero-padding convention — per research open question #2)
    - Every item has non-empty korean, english, romanization strings
    - Every item speech_level is one of the 4 valid values
    - Every item conjugation_type is one of the 5 valid values
    - Every item topics array references IDs that exist in TOPICS

    **Grammar data integrity:**
    - All IDs unique and match `/^grm-\d{3}$/`
    - Every point has at least 1 example sentence
    - Every example has non-empty korean and english

    **Lesson data integrity:**
    - All IDs unique and match `/^les-\d{3}$/`
    - Every lesson's vocab_ids all exist in VOCAB
    - Every lesson's grammar_ids all exist in GRAMMAR

    **Cross-collection uniqueness:**
    - No ID appears in more than one collection (even across types — defense against copy-paste)

    This test file acts as the runtime complement to the build-time `satisfies` checking. TypeScript catches shape errors; tests catch semantic errors (duplicates, dangling references, formatting).
  </action>
  <verify>
    <automated>cd C:/dev/Hanbuddy && npx vitest run src/types/content.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <done>All content data validation tests pass. ID uniqueness enforced. Cross-references verified. Zero-padding convention validated by regex.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero TypeScript errors
2. `npx vitest run src/types/content.test.ts` — all tests pass
3. `npx tsc --noEmit` — clean (no errors)
4. Project folder structure matches locked decision: components/, pages/, store/, data/, utils/, hooks/, types/, engine/
</verification>

<success_criteria>
- Vite dev server starts and renders "Hanbuddy" heading
- TypeScript build passes with all content types and sample data
- Content validation tests pass (ID uniqueness, cross-references, formatting)
- A deliberately malformed VocabItem (e.g., missing english field) causes a TypeScript build error when satisfies is applied
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
